name: gallos_app_new
description: "Casta de Gallos - iOS Build"
publish_to: 'none'

version: 1.1.4+804

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # SOLO LO MÍNIMO INDISPENSABLE PARA iOS:
  provider: ^6.0.5
  http: ^1.1.0
  shared_preferences: ^2.2.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - lib/data/mock/
    - assets/images/